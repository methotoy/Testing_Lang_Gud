ngDescribe({
    name: 'Test app-side-nav component',
    modules: 'app',
    element: '<app-side-nav></app-side-nav>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
